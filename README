**项目名称**
- **基于STM32的二维码室内定位系统**：使用 STM32F407ZGT6 最小系统板 + K210 视觉模块实现室内定位与导航。

**项目简介**
- 本毕业设计实现了一套基于二维码识别的室内定位与导航系统。系统通过 K210 视觉模块识别环境中的二维码，主控使用 STM32F407ZGT6 进行运动控制与定位融合，驱动为 TB6612 四路电机驱动模块，采用 520 编码器麦轮套装获取轮速与里程计信息。适用于移动机器人（小车）室内路径定位与导航实验平台。

**主要特性**
- 实时二维码检测与识别（K210）。
- 基于编码器的里程计和运动控制（STM32）。
- 电机驱动与速度控制（TB6612）。
- 支持在最小系统板上运行并通过 SWD 或串口烧录/调试。

**硬件清单**
- 主控：STM32F407ZGT6 最小系统板（或等效 STM32F407 开发板）。
- 视觉模块：K210（用于二维码识别，运行人脸/图像处理/QR 识别脚本）。
- 轮组：520 编码器麦轮套装（带编码器反馈）。
- 驱动：TB6612 四路电机驱动模块（稳压供电并驱动直流电机）。
- 供电：适配的电池或稳压电源（请参照电机与驱动额定电压）。
- 编程/调试：ST-Link / J-Link（或通过串口在 bootloader 情况下刷写）。

**软件组件**
- STM32 固件：基于 HAL 库的控制与外设驱动，代码位于 [Core/Src/main.c](Core/Src/main.c) 及相关目录。
- K210 模块：位于 `K210/` 下的识别脚本（例如 `QRRecognize.py`），在 K210 或配套主控运行环境上执行。
- 工具链：基于 Makefile 与 VSCode 任务（仓库根目录包含 `Makefile`，工作区配置带有 `build`/`flash` 等任务）。

**电路与连接（概述）**
- 将编码器信号接入 STM32 的定时器输入捕获端口，用于计数与速度计算。确保同时接地。
- TB6612 的 PWM 输入接至 STM32 的定时器 PWM 输出，AIN/BIN 引脚接 GPIO 控制方向。
- 将 K210 的串口或 USB/UART（视模块型号而定）与 STM32 串口相连，用于图像识别结果与主控之间通信；或者 K210 单独处理并通过串口发送识别数据。
- 电源：给电机与驱动模块提供独立稳压电源，注意共地。K210 与 STM32 的电源电压按模块要求接入。

（注：具体引脚分配与接线请参考工程中的引脚配置与 `main.h` / 硬件原理图）

**快速开始**
- 克隆仓库并打开到 VSCode：

	- `git clone <repo-url>`
	- 在 VSCode 中打开工程根目录。

- 使用 VSCode 任务构建与烧录（推荐）：

	- 打开命令面板 -> `Tasks: Run Task` -> 选择 `build` 来编译。
	- 选择 `flash` 或 `build and flash` 来烧录到目标板（需要已配置的上传工具）。

- 使用命令行（若支持 Makefile）：

	- `make` —— 编译固件（如果 Makefile 支持此命令）。
	- 使用你惯用的烧录工具（如 ST-Link CLI、OpenOCD）将生成的固件刷入 MCU。

**运行与调试**
- 连接 ST-Link/J-Link，使用调试器在 VSCode 或 Keil/STM32CubeIDE 中下载并单步调试。
- 通过串口监视（串口波特率请参考 `Core/Inc/main.h` 或代码中的配置）观察运行日志与识别数据。

**使用说明（高层流程）**
- K210 捕获图像并进行二维码识别，识别到坐标或 ID 后将结果发送给 STM32。
- STM32 接收识别结果并结合编码器里程计，进行定位融合与运动规划。
- MCU 通过 PWM 控制 TB6612 驱动电机执行导航动作，并持续调整以达到目标位置。

**调试与常见问题**
- 无法识别二维码：确认 K210 摄像头对焦、光照、二维码尺寸与视角；可以在 `K210/QRRecognize.py` 中打开调试输出。
- 编码器计数不准确：检查编码器连接是否稳定，消抖或滤波；确认定时器配置与分频设置正确。
- 电机抖动或无响应：检查 TB6612 电源、使能引脚和 PWM 信号频率/占空比是否正常；确认共地连接。

**工程结构（简要）**
- `Core/`：STM32 主程序与 HAL 配置（`Core/Src`、`Core/Inc`）。
- `Drivers/`：第三方库与 HAL 驱动。
- `K210/`：K210 视觉识别脚本与模型文件。
- `build/`：构建输出目录。

**许可与致谢**
- 本项目为毕业设计作品，代码与资源请遵循作者意愿分享（如需对外发布，请联系作者确认许可）。
- 感谢 STM32 HAL 库、K210 社区与开源项目提供支持。

**作者**
- 项目：基于STM32的二维码室内定位系统
- 作者：RayanceKing

如果需要我把 README 转成 Markdown 格式的 `README.md`、加入示意接线图或示例配置（如具体 UART/PWM 引脚表），我可以继续补充。

